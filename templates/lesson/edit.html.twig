{% extends 'base.html.twig' %}

{% block title %}Edit Lesson{% endblock %}

{% block body %}
    <h1 class="font-weight-bold m-3">{{ lesson.name }}</h1>
    <h2 class="m-3"><a href="{{ path('course_show', {'id': lesson.course.id}) }}">{{ lesson.course.name }}</a></h2>

    {{ include('lesson/_form.html.twig', {'button_label': 'Сохранить'}) }}
    <div class="line mt-4 mb-4"></div>
    <div class="d-flex">
    <a href="{{ path('course_show', {'id':lesson.course.id }) }}">К списку уроков</a>
    {{ include('lesson/_delete_form.html.twig') }}
    </div>
{% endblock %}
