{% extends 'base.html.twig' %}

{% block title %}Lesson{% endblock %}

{% block body %}
    <h1>Lesson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ lesson.name }}</td>
            </tr>
            <tr>
                <th>Course</th>
                <td><a href="{{ path('course_show', {'id': lesson.course.id}) }}">{{ lesson.course.name }}</a> </td>
            </tr>
            <tr>
                <th>Material</th>
                <td>{{ lesson.material }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('course_show', {'id': lesson.course.id}) }}">back to list</a>

    <a href="{{ path('lesson_edit', {'id': lesson.id}) }}">edit</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
